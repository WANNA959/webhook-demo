apiVersion: apps/v1
kind: Deployment
metadata:
  name: sleep
  namespace: webhook-demo
spec:
  selector:
    matchLabels:
      app: sleep
  template:
    metadata:
      namespace: webhook-demo
      labels:
        app: sleep
    spec:
      containers:
        - name: sleep
          image: busybox
          command: ["/bin/sleep","infinity"]
          imagePullPolicy: IfNotPresent